{
  "name": "wazuh-operator-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Dev tooling (Prettier) used by `make helm-docs` to format the generated Helm chart READMEs.",
  "scripts": {
    "format": "prettier --write \"charts/*/README.md\"",
    "format:check": "prettier --check \"charts/*/README.md\""
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
