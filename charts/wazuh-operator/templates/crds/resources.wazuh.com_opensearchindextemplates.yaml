{{- if .Values.crds.install }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: opensearchindextemplates.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: OpenSearchIndexTemplate
    listKind: OpenSearchIndexTemplateList
    plural: opensearchindextemplates
    shortNames:
    - osidxt
    singular: opensearchindextemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.indexPatterns
      name: Patterns
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.driftDetected
      name: Drift
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: OpenSearchIndexTemplate is the Schema for the opensearchindextemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchIndexTemplateSpec defines the desired state of
              OpenSearchIndexTemplate
            properties:
              _meta:
                description: Metadata about the index template
                type: object
                x-kubernetes-preserve-unknown-fields: true
              clusterRefs:
                description: WazuhCluster reference
                items:
                  description: |-
                    WazuhClusterRef references a WazuhCluster by name and namespace.
                    Both fields are mandatory; cross-namespace references are supported and
                    require the consuming CR to opt into multi-cluster propagation via
                    ClusterRefs lists.
                  properties:
                    name:
                      description: Name of the WazuhCluster resource
                      minLength: 1
                      type: string
                    namespace:
                      description: Namespace of the WazuhCluster resource
                      minLength: 1
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                - namespace
                x-kubernetes-list-type: map
              composedOf:
                description: Component templates to be applied
                items:
                  type: string
                type: array
              dataStream:
                description: Data stream configuration
                properties:
                  hidden:
                    description: Hidden flag for data stream
                    type: boolean
                  timestamp_field:
                    description: Timestamp field for data stream
                    properties:
                      name:
                        default: '@timestamp'
                        description: Name of the timestamp field
                        type: string
                    required:
                    - name
                    type: object
                type: object
              indexPatterns:
                description: Index patterns that the template applies to
                items:
                  type: string
                minItems: 1
                type: array
              priority:
                description: Priority of the index template
                format: int32
                type: integer
              template:
                description: Template definition
                properties:
                  aliases:
                    additionalProperties:
                      description: IndexAlias defines an index alias
                      properties:
                        filter:
                          description: Filter for the alias
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        index_routing:
                          description: Index routing
                          type: string
                        is_write_index:
                          description: Is write index
                          type: boolean
                        search_routing:
                          description: Search routing
                          type: string
                      type: object
                    description: Aliases for the index template
                    type: object
                  mappings:
                    description: Mappings for the index template
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  settings:
                    description: Settings for the index template
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              version:
                description: Version of the index template
                format: int64
                type: integer
            required:
            - clusterRefs
            - indexPatterns
            type: object
          status:
            description: OpenSearchIndexTemplateStatus defines the observed state
              of OpenSearchIndexTemplate
            properties:
              clusterStatuses:
                description: ClusterStatuses reports per-target-cluster reconciliation
                  state.
                items:
                  description: |-
                    OpenSearchClusterStatus reports the reconciliation state for one target
                    OpenSearch cluster of a multi-cluster CR (User, Role, Tenant, etc.).
                    The aggregate Status.Phase reflects the worst case across entries.
                  properties:
                    lastAppliedHash:
                      description: LastAppliedHash is the hash of the last spec applied
                        to this cluster
                      type: string
                    lastSyncTime:
                      description: LastSyncTime is the last time the resource was
                        synced to this cluster
                      format: date-time
                      type: string
                    message:
                      description: Message provides additional information about this
                        cluster's state
                      type: string
                    name:
                      description: Name of the target WazuhCluster
                      type: string
                    namespace:
                      description: Namespace of the target WazuhCluster
                      type: string
                    phase:
                      description: Phase on this cluster (Pending, Ready, Failed,
                        Conflict)
                      enum:
                      - Pending
                      - Ready
                      - Failed
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                - namespace
                x-kubernetes-list-type: map
              conditions:
                description: Conditions represent the latest available observations
                  of the index template's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              conflictsWith:
                description: ConflictsWith is the namespace/name of a conflicting
                  CRD
                type: string
              driftDetected:
                description: DriftDetected indicates if manual modification was detected
                type: boolean
              lastAppliedHash:
                description: LastAppliedHash is the hash of the last applied spec
                  for drift detection
                type: string
              lastDriftTime:
                description: LastDriftTime is when drift was last detected
                format: date-time
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last synced to
                  OpenSearch
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              ownershipClaimed:
                description: OwnershipClaimed indicates if this CRD owns the OpenSearch
                  resource
                type: boolean
              phase:
                description: Phase represents the current phase of the index template
                enum:
                - Pending
                - Ready
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
