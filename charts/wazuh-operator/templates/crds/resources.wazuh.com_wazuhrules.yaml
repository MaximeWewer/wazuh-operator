{{- if .Values.crds.install }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: wazuhrules.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: WazuhRule
    listKind: WazuhRuleList
    plural: wazuhrules
    shortNames:
    - wrule
    singular: wazuhrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ruleName
      name: Rule
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.targetNodes
      name: Target
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: WazuhRule is the Schema for the wazuhrules API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WazuhRule
            properties:
              clusterRefs:
                description: |-
                  ClusterRefs lists the WazuhCluster instances the rule must be applied to.
                  Each entry must specify both name and namespace. The CR can live in any
                  namespace; rule ConfigMaps are created in each target cluster's namespace.
                items:
                  description: |-
                    WazuhClusterRef references a WazuhCluster by name and namespace.
                    Both fields are mandatory; cross-namespace references are supported and
                    require the consuming CR to opt into multi-cluster propagation via
                    ClusterRefs lists.
                  properties:
                    name:
                      description: Name of the WazuhCluster resource
                      minLength: 1
                      type: string
                    namespace:
                      description: Namespace of the WazuhCluster resource
                      minLength: 1
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                - namespace
                x-kubernetes-list-type: map
              description:
                description: Description of the rule set
                type: string
              groups:
                description: Groups are the rule groups this rule set belongs to
                items:
                  type: string
                type: array
              ifGroup:
                description: IfGroup references parent rule groups
                items:
                  type: string
                type: array
              ifSID:
                description: IfSID references parent rule IDs
                items:
                  format: int32
                  type: integer
                type: array
              level:
                description: Level specifies the rule level (0-15)
                format: int32
                maximum: 15
                minimum: 0
                type: integer
              overwrite:
                description: Overwrite determines if this rule should overwrite existing
                  rules
                type: boolean
              priority:
                default: 500
                description: Priority determines the order in which rules are applied
                  (lower values = higher priority)
                format: int32
                maximum: 1000
                minimum: 0
                type: integer
              ruleID:
                description: RuleID is the starting rule ID for rules in this set
                format: int32
                maximum: 999999
                minimum: 100000
                type: integer
              ruleName:
                description: RuleName is the name of the rule
                type: string
              rules:
                description: Rules contain the actual rule definitions in XML format
                type: string
              targetNodes:
                default: all
                description: TargetNodes specifies which nodes should receive this
                  rule (master, workers, or all)
                enum:
                - master
                - workers
                - all
                type: string
            required:
            - clusterRefs
            - ruleName
            - rules
            type: object
          status:
            description: status defines the observed state of WazuhRule
            properties:
              clusterStatuses:
                description: ClusterStatuses reports per-target-cluster reconciliation
                  state.
                items:
                  description: RuleClusterStatus reports the reconciliation state
                    for a single target cluster.
                  properties:
                    appliedToNodes:
                      description: AppliedToNodes lists the nodes where this rule
                        has been applied on this cluster
                      items:
                        type: string
                      type: array
                    configMapRef:
                      description: ConfigMapRef references the ConfigMap created in
                        this cluster's namespace
                      properties:
                        key:
                          description: Key in the ConfigMap
                          type: string
                        name:
                          description: Name of the ConfigMap
                          type: string
                        namespace:
                          description: Namespace of the ConfigMap
                          type: string
                      required:
                      - name
                      type: object
                    lastAppliedTime:
                      description: LastAppliedTime is the last time the rule was applied
                        to this cluster
                      format: date-time
                      type: string
                    message:
                      description: Message provides additional information about this
                        cluster's state
                      type: string
                    name:
                      description: Name of the target WazuhCluster
                      type: string
                    namespace:
                      description: Namespace of the target WazuhCluster
                      type: string
                    phase:
                      description: Phase on this cluster
                      enum:
                      - Pending
                      - Applied
                      - Failed
                      - Updating
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                - namespace
                x-kubernetes-list-type: map
              conditions:
                description: Conditions represent the latest aggregate observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message provides additional aggregate information about
                  the current state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed rule
                format: int64
                type: integer
              phase:
                description: Phase is the aggregate phase across all target clusters.
                enum:
                - Pending
                - Applied
                - Failed
                - Updating
                type: string
              validationErrors:
                description: ValidationErrors contains any validation errors encountered
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
