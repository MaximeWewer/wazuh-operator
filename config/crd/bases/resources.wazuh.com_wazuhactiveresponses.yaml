---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: wazuhactiveresponses.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: WazuhActiveResponse
    listKind: WazuhActiveResponseList
    plural: wazuhactiveresponses
    shortNames:
    - war
    singular: wazuhactiveresponse
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.location
      name: Location
      type: string
    - jsonPath: .spec.targetNodes
      name: Target
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: WazuhActiveResponse is the Schema for the wazuhactiveresponses
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WazuhActiveResponse
            properties:
              agentID:
                description: |-
                  AgentID is the target agent id (<agent_id>), required when Location is
                  "defined-agent".
                type: string
              clusterRefs:
                description: |-
                  ClusterRefs lists the WazuhCluster instances the active response must be applied to.
                  Each entry must specify both name and namespace. The CR can live in any
                  namespace; the script ConfigMap is created in each target cluster's namespace.
                items:
                  description: |-
                    WazuhClusterRef references a WazuhCluster by name and namespace.
                    Both fields are mandatory; cross-namespace references are supported and
                    require the consuming CR to opt into multi-cluster propagation via
                    ClusterRefs lists.
                  properties:
                    name:
                      description: Name of the WazuhCluster resource
                      minLength: 1
                      type: string
                    namespace:
                      description: Namespace of the WazuhCluster resource
                      minLength: 1
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                - namespace
                x-kubernetes-list-type: map
              disabled:
                default: false
                description: |-
                  Disabled sets <disabled>yes</disabled> on the active-response block, keeping
                  the command defined but inactive.
                type: boolean
              extraArgs:
                description: ExtraArgs are static arguments passed to the script (<extra_args>).
                type: string
              level:
                description: Level triggers the response when an alert of at least
                  this severity fires (<level>).
                format: int32
                maximum: 16
                minimum: 0
                type: integer
              location:
                default: local
                description: |-
                  Location selects where the response runs (<location>):
                  "local" (the agent that triggered the alert), "server" (the manager),
                  "defined-agent" (the agent in AgentID), or "all" (every agent).
                enum:
                - local
                - server
                - defined-agent
                - all
                type: string
              name:
                description: |-
                  Name is the active response identifier. It is used as the ossec.conf
                  <command> <name> and referenced by the <active-response> <command>.
                  It is also the base of the script filename (see ScriptName).
                maxLength: 50
                pattern: ^[a-zA-Z0-9_-]+$
                type: string
              repeatedOffenders:
                description: |-
                  RepeatedOffenders is the escalating timeout schedule in minutes for repeat
                  offenders (<repeated_offenders>), rendered comma-separated (e.g. 30,60,120).
                items:
                  format: int32
                  type: integer
                type: array
              rulesGroup:
                description: RulesGroup triggers the response for alerts in this rule
                  group (<rules_group>).
                type: string
              rulesID:
                description: RulesID triggers the response for the listed rule IDs
                  (<rules_id>), rendered comma-separated.
                items:
                  format: int32
                  type: integer
                type: array
              script:
                description: |-
                  Script is the content of the active response executable. It is mounted with
                  mode 0750 (root:wazuh) at /var/ossec/active-response/bin/<script>. The first
                  line must be a shebang (e.g. "#!/usr/bin/env python3" or "#!/bin/sh").
                type: string
              scriptExtension:
                description: |-
                  ScriptExtension is the optional file extension (without the leading dot)
                  appended to the script filename, e.g. "sh" → <name>.sh, "py" → <name>.py.
                  The <executable> tag always matches the resulting filename.
                maxLength: 10
                pattern: ^[a-zA-Z0-9]+$
                type: string
              targetNodes:
                default: all
                description: TargetNodes selects which manager nodes receive the active
                  response (master, workers, or all).
                enum:
                - master
                - workers
                - all
                type: string
              timeout:
                description: |-
                  Timeout is the number of seconds after which a stateful response is reverted
                  (<timeout>). Requires TimeoutAllowed on the command.
                format: int32
                minimum: 0
                type: integer
              timeoutAllowed:
                default: false
                description: |-
                  TimeoutAllowed enables the stateful timeout mechanism for the command
                  (<timeout_allowed>). Set to true for scripts that revert their action after
                  <timeout> seconds (add/delete style).
                type: boolean
            required:
            - clusterRefs
            - name
            - script
            type: object
          status:
            description: status defines the observed state of WazuhActiveResponse
            properties:
              clusterStatuses:
                description: ClusterStatuses reports per-target-cluster reconciliation
                  state.
                items:
                  description: ActiveResponseClusterStatus reports the reconciliation
                    state for a single target cluster.
                  properties:
                    appliedToNodes:
                      description: AppliedToNodes lists the nodes where this active
                        response has been applied on this cluster.
                      items:
                        type: string
                      type: array
                    configMapRef:
                      description: ConfigMapRef references the script ConfigMap created
                        in this cluster's namespace.
                      properties:
                        key:
                          description: Key in the ConfigMap
                          type: string
                        name:
                          description: Name of the ConfigMap
                          type: string
                        namespace:
                          description: Namespace of the ConfigMap
                          type: string
                      required:
                      - name
                      type: object
                    lastAppliedTime:
                      description: LastAppliedTime is the last time the active response
                        was applied to this cluster.
                      format: date-time
                      type: string
                    message:
                      description: Message provides additional information about this
                        cluster's state.
                      type: string
                    name:
                      description: Name of the target WazuhCluster.
                      type: string
                    namespace:
                      description: Namespace of the target WazuhCluster.
                      type: string
                    phase:
                      description: Phase on this cluster.
                      enum:
                      - Pending
                      - Applied
                      - Failed
                      - Updating
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                - namespace
                x-kubernetes-list-type: map
              conditions:
                description: Conditions represent the latest aggregate observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message provides additional aggregate information about
                  the current state.
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed active response.
                format: int64
                type: integer
              phase:
                description: Phase is the aggregate phase across all target clusters.
                enum:
                - Pending
                - Applied
                - Failed
                - Updating
                type: string
              validationErrors:
                description: ValidationErrors contains any validation errors encountered.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
